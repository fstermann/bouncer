<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<!-- Menu bar only: no Dock icon, no main menu. -->
	<key>LSUIElement</key>
	<true/>
	<!-- Sparkle. The feed is regenerated on every release and attached to it, so
	     `latest/download` always resolves to the newest one. The repository is substituted
	     at build time (SU_FEED_URL, defaulted in project.yml) so the feed and the release
	     workflow's enclosure URLs cannot drift apart across a rename, transfer or fork. -->
	<key>SUFeedURL</key>
	<string>$(SU_FEED_URL)</string>
	<!-- Public half of the EdDSA key the release workflow signs updates with. Replace with
	     the output of Sparkle's `generate_keys`; updates are refused until it matches. -->
	<key>SUPublicEDKey</key>
	<string>1Sn1HyZTHiNEUqmxAz6JaIockKQQvnbnmIbag0Cedw8=</string>
	<!-- Set explicitly so Sparkle never puts up its own second-launch permission dialog:
	     Settings is the only place updates are configured. Both are defaults the user
	     overrides there, after which Sparkle stores their choice itself. -->
	<key>SUEnableAutomaticChecks</key>
	<true/>
	<key>SUScheduledCheckInterval</key>
	<integer>86400</integer>
	<key>SUAutomaticallyUpdate</key>
	<false/>
	<key>NSHumanReadableCopyright</key>
	<string>MIT Licensed</string>
</dict>
</plist>
